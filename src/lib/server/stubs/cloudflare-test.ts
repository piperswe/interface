// Stub for `cloudflare:test` so the SSR build can run in Node.js.
// Only used during the Vite SSR bundling step.
export const env = {};
export function runDurableObjectAlarm() {}
export function runInDurableObject() {}
